
trigger:
- feature/Docker

resources:
- repo: self

pool:
  name: 'Default'
  demands:
  - agent.name -equals agentclass

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build and Push Docker Image
  jobs:
  - job: Build
    displayName: Build
    steps:
    - task: Docker@2
      displayName: Build and push Docker image
      inputs:
        containerRegistry: 'classtrackapi'
        command: 'build'
        Dockerfile: '**/backend/Dockerfile'
        arguments: '-t classtrackapi.azurecr.io/classtrack-api:latest'
    - task: Docker@2
      inputs:
        containerRegistry: 'classtrackapi'
        repository: 'classtrack-api'
        command: 'push'
        tags: 'latest'
    - task: AzureWebAppContainer@1
      inputs:
        azureSubscription: 'Azure for Students(8c757b97-b103-445f-9aa8-d6cff021ddcf)'
        appName: 'ClassTrack-API-Alumnos'
        containers: 'classtrack-api'
        appSettings: '-Port 3000'